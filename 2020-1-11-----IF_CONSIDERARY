1.增加流程预检接口，用于检查文件格式，接口由子工程实现，当文件格式不正确时，发起虚流程，而不是实流程。
2.用户可通过预检查流程，生成流程参数，该参数（JobParameters）在Job中可接收。
3.通过注解来加载扩展组件以及接口实现，而不是通过指定Class的方式，配置时通过注解名称进行匹配。
4.启动流程时，将Job的执行服务实例注册到Flow_partition上，以及服务地址。并且在服务实例内存同时注册Job实例。
同时启动一个Worker用于监控Job实例是否在服务实例内存中存在，如果不存在，则发起重试（由监控服务重新发起分片调度）。
（flow_partition 需要增加重试次数,auto_restart，当超过一定重试次数则放弃重试）
当服务实例重启后，实例内存中不再存在流程实例，则worker通知monitor发起重试。
